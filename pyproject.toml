[project]
name = "schematic-dagster"
version = "0.1.0"
description = "Schematic Dagster"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Dagster dependencies
    "dagster==1.9.11",
    "dagster-postgres",
    "dagster-k8s",
    "dagster-graphql",
    "dagster-webserver",
]
